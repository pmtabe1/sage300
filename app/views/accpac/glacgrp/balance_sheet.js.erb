<% if params[:year].present? %>
  $("#as_of").html("<%= escape_javascript(render partial: 'balance_sheet_as_of') %>");
  $("#filters").hide();
<% else %>
  $("#balances").html("<%= escape_javascript(render partial: 'balance_sheet') %>");
  $("#filters").hide();
<% end %>
